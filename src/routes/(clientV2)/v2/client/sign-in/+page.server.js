

// 'actions' object that has a key:value = default:handlerFunction

// not in use currently
export const actions = {
    default: async ({request}) => {
        const data = await request.formData();

}}